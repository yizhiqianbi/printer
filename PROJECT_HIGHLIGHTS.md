# Printer Project Highlights

## 一句话定位

`Printer` 是一个面向前端 artifact 的本地生成与展示工具：它把意图、页面素材和玩法灵感转成可打开、可验证、可从零 fake、可继续 remix 的网页产物。

它不是传统后台项目，也不只是一个静态页面集合。更准确地说，它是一套“从想法到可演示前端结果”的轻量工作台。

## 核心亮点

### 架构对应关系

这些亮点分别对应仓库里的三条实际链路：通用网页生成能力在 `web_printer_selenium.py` 和 `src/`；小游戏赝品库在 `tools/generate_fake_library.mjs` 和 `output/`；从零 Fake 与 Remix 发布链路在 `tools/remix_harness_server.py`、`tests/test_remix_harness.py` 和 `output/remix_manifest.json`。

因此对外介绍时，可以把 `output/index.html` 当作最终展示面，但维护时要回到生成器、harness 和 verifier，而不是只改生成后的 HTML。

### 1. Artifact-first 工作流

项目的输出目标很明确：生成用户能直接打开、演示和检查的前端产物。无论是单页 HTML、多页 HTML，还是项目骨架，最终都会落到文件系统里，而不是停留在聊天或设计稿层面。

这种方式适合快速复刻、原型验证、小游戏实验和交互 demo，因为每次生成后都能立刻进入“打开、试玩、验证、继续改”的闭环。

### 2. 多输入意图理解

`Web Printer Pipeline` 支持把意图描述、URL、截图和 MHTML 放在同一个任务里处理。项目会先解析输入，再采集页面结构和视觉信息，最后根据复杂度选择输出形态。

这让它不局限于“仿一个网页”，而是可以表达更具体的目标，例如强调某个转化路径、复刻某类交互、合并多个参考页面，或根据截图补全一个可运行版本。

### 3. 复杂度驱动输出

项目会根据组件数量、交互密度、页面数量和数据流复杂度决定生成策略。简单任务可以落成一个自包含 HTML，复杂任务可以生成多页面结构或项目骨架。

这样做的好处是范围可控：小任务不被工程化过度包装，大任务又不会被压成难维护的单文件。

### 4. Fake Game Library 展示面

`Fake Game Library` 是项目最直观的展示层。它把热门小游戏机制做成离线、竖屏、可交互的赝品游戏页，并通过一个类似短视频/小游戏平台的首页聚合起来。

当前展示页强调三件事：

- 先用封面和瀑布流让用户快速理解“这里有很多可玩的东西”。
- 再用搜索、分类和数量反馈让用户快速找到某类玩法。
- 最后用沉浸式滑动 feed 让每个游戏都能直接试玩，而不是只看缩略图。

### 5. UI 内置从零 Fake

首页现在不是只能浏览已有库。它把 `Fake 一款` 作为一级入口展示出来：用户输入玩法、题材和短局目标，就可以让本地 harness 从零生成一个新游戏草稿。

这条路径适合展示“创作能力”本身：不需要先选中已有游戏，不需要手工改 generator，就能先得到可预览、可命名、可发布的单文件小游戏。发布后它会进入同一个 manifest、瀑布流和滑动 feed，显示为 `@Fake`。

### 6. Remix-ready 设计

每个 fake game 都不是一次性页面。它们保留了统一的 artifact 元数据和页面契约，可以被 Remix Harness 读取、生成草稿、预览并发布成新的变体。

这使项目可以持续扩展：新增一个基础玩法后，可以很快派生办公室版、夜班版、通勤版或其他本地语境二创，而不用重新搭一套展示和发布流程。

### 7. Copy + Remix 的产品闭环

项目可以分成两个核心 Part 来介绍：

- `Copy / 抄袭`：把外部小游戏或模型思路拆成可运行的本地游戏模型，沉淀成 fake-library 资产。
- `Remix / 二创`：在已有模型、agent description 和资产库基础上继续变体生成。

两者不是割裂功能。Copy 负责把流行玩法转成可控资产，Remix 负责继续扩散题材、节奏和展示形态；从零 Fake 则补足“现场创作一款”的 demo 入口。

### 8. 本地优先、低依赖

Fake game 页面默认是离线、自包含、移动端优先的 HTML。它们不依赖外部 CDN、远程图片或字体，适合在本地静态服务里直接打开，也适合作为可归档的生成物提交。

这降低了 demo 失效风险，也让 verifier 可以用静态契约检查大部分问题。

### 9. 验证意识内置

项目不是只追求“生成成功”。它配套了 fake-library verifier、游戏交互契约检查、remix/fake 输出检查和 harness 单元测试。

这种验证方式让每次小改动都有明确验收口径：页面要能打开，结构要符合约定，关键方法要存在，首页要能注册生成物、从零 fake 产物和 remix 产物。

## 适合展示的讲法

如果要向别人介绍这个项目，可以按这个顺序讲：

1. `Printer` 的目标是把意图直接变成可运行的前端 artifact。
2. 它有通用网页复刻 pipeline，也有一个更容易展示的小游戏赝品库。
3. 小游戏库里的每个页面都是离线可玩的独立 HTML。
4. 首页像一个最终展示面：能搜索、筛选、滑动试玩，也能从零 fake 或进入 remix。
5. 产品核心是 Copy 和 Remix：先把玩法模型转成本地游戏资产，再继续二创扩散。
6. 新玩法不是手工散落在 `output/`，长期版本要进生成器，再通过 verifier 验收。

## 适合强调的差异点

- 它关注“可运行结果”，不是只产出说明文字。
- 它把生成、展示、从零 fake、remix 和验证连成一个闭环。
- 它允许小范围快速迭代，不强行引入额外框架或依赖。
- 它能把当下流行玩法沉淀成可本地归档的 fake game artifact。
- 它的产物既能给人演示，也能给脚本验证。

## Demo 入口建议

对外展示时，优先打开 fake-library 首页：

```text
output/index.html
```

推荐演示路径：

1. 展示首页封面拼贴和瀑布流，说明这是可玩的 artifact 库。
2. 点击 `Fake 一款`，说明它可以不选源游戏、直接从玩法 prompt 生成新草稿。
3. 搜索一个关键词，例如“停车”或“Remix”。
4. 切换 `.io`、`Remix` 或“解谜/排序”分类，展示实时过滤。
5. 点击“进入最终版”或任意卡片，进入沉浸式滑动 feed。
6. 打开 Remix 面板，说明本地 harness 启动后可以基于当前游戏生成并发布新变体。

## 和项目指南的区别

`PROJECT_DOCUMENTATION.md` 适合维护者阅读，重点是目录、命令、环境变量、生成流程和验收方式。

本文件适合展示、汇报或快速介绍项目价值，重点是项目亮点、产品感和 demo 讲法。
